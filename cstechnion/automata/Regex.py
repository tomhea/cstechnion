"""
TODO (after finishing NFAe):
    Regex.EmptyGroup, Regex.EmptyWord
    Regex(str)
    Regex() + Regex()   // Union
    Regex() @ Regex()   // Concatenation
    Regex() ^ int       // [int] times self concatenation
    Regex() * int       // iteration (at-least [int] times). also: Regex().iteration()
    str(Regex()) --> minimized/un-minimized expression
    Regex().to_NFAe
"""
